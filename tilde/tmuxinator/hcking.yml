# ~/.tmuxinator/hcking.yml

project_name: hcking
project_root: ~/code/hcking
pre: vagrant up
windows:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - #empty, will just run plain bash
